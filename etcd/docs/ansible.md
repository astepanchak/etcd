# Install components using Ansible

We will wait until Terraform has finished and the Instances will become available.

Then we will run Ansible commands from `./ansible` subdirectory.

```
$ ansible-playbook site.yaml
```
